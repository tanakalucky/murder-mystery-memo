---
phase: 01-canvas-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - index.html
  - src/app/index.tsx
  - src/app/styles/index.css
  - src/app/routes/routes.tsx
  - src/shared/api/index.ts
  - e2e/todo.spec.ts
autonomous: true
requirements:
  - CANVAS-01
must_haves:
  truths:
    - "bun run typecheck passes with zero errors"
    - "bun run build succeeds with no Convex/Clerk/Cloudflare/wouter imports"
    - "No console errors from dead packages at runtime"
  artifacts:
    - path: "package.json"
      provides: "Clean dependency list without dead packages"
      contains: "@xyflow/react"
    - path: "vite.config.ts"
      provides: "Build config without cloudflare plugin"
    - path: "src/app/index.tsx"
      provides: "Simplified entry point with no Clerk/Convex providers"
    - path: "src/app/routes/routes.tsx"
      provides: "Simple route rendering CanvasPage"
  key_links:
    - from: "src/app/index.tsx"
      to: "src/app/routes/routes.tsx"
      via: "import { Routes }"
      pattern: "import.*Routes.*from.*routes"
    - from: "src/app/routes/routes.tsx"
      to: "src/pages/canvas/index.ts"
      via: "import { CanvasPage }"
      pattern: "import.*CanvasPage.*from.*pages/canvas"
---

<objective>
Remove all dead dependencies (Convex, Clerk, Cloudflare, wouter, next-themes, dotenvx, fontsource-noto-sans) from the codebase and install @xyflow/react. Clean up all source files that import these packages. Rename pages/home to pages/canvas.

Purpose: Establish a clean codebase free of dead imports so the canvas can be built on a stable foundation.
Output: A buildable, type-checked project with @xyflow/react installed and zero references to removed packages.
</objective>

<execution_context>
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/murder-mystery-memo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/murder-mystery-memo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-foundation/01-CONTEXT.md
@.planning/phases/01-canvas-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead packages and clean package.json/vite.config.ts/scripts</name>
  <files>package.json, vite.config.ts, index.html</files>
  <read_first>
    - package.json (current dependencies and scripts)
    - vite.config.ts (current plugins)
    - index.html (title and root div)
  </read_first>
  <action>
1. Remove the following packages from package.json using bun:
   ```bash
   bun remove convex @clerk/react @clerk/testing @cloudflare/vite-plugin wrangler @dotenvx/dotenvx next-themes wouter @fontsource-variable/noto-sans
   ```

2. Install @xyflow/react (per CANVAS-01):

   ```bash
   bun add @xyflow/react
   ```

3. Edit package.json scripts section — replace the following scripts with their cleaned versions:
   - Delete `"cf-typegen": "wrangler types"` entirely
   - Delete `"deploy": "wrangler deploy"` entirely
   - Change `"check"` from `"bun typecheck && bun run build && wrangler deploy --dry-run"` to `"bun typecheck && bun run build"`
   - Change `"dev"` from `"dotenvx run -f .env.dev -- vite"` to `"vite"`
   - Change `"preview"` from `"dotenvx run -f .env.dev -- bun run build && vite preview"` to `"bun run build && vite preview"`
   - Change `"test"` from `"vitest --project unit & vitest --project browser & dotenvx run -f .env.dev -- bun run playwright test"` to `"vitest --project unit & vitest --project browser & bun run playwright test"`

4. Remove the `"cloudflare"` top-level key from package.json (the entire object with label, products, categories, icon_urls, preview_image_url, publish).

5. Change package.json `"name"` from `"app-boilerplate"` to `"murder-mystery-memo"`.

6. Edit vite.config.ts:
   - Remove `import { cloudflare } from "@cloudflare/vite-plugin";`
   - Change `plugins: [react(), cloudflare(), tailwindcss()]` to `plugins: [react(), tailwindcss()]`

7. Edit index.html:
   - Change `<title>Todoアプリ</title>` to `<title>Murder Mystery Memo</title>`
     </action>
     <verify>
     <automated>cd /Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/murder-mystery-memo && grep -c "convex\|@clerk\|@cloudflare\|wrangler\|dotenvx\|next-themes\|wouter\|fontsource-variable/noto-sans" package.json; echo "expect 0" && grep "@xyflow/react" package.json && grep -v "cloudflare" vite.config.ts | grep -q "plugins" && echo "vite ok"</automated>
     </verify>
     <acceptance_criteria>
   - package.json does NOT contain strings "convex", "@clerk", "@cloudflare", "wrangler", "dotenvx", "next-themes", "wouter", "fontsource-variable/noto-sans"
   - package.json contains `"@xyflow/react"` in dependencies
   - package.json scripts.dev equals `"vite"`
   - package.json scripts.check equals `"bun typecheck && bun run build"`
   - package.json does not have a "cloudflare" top-level key
   - package.json name is "murder-mystery-memo"
   - vite.config.ts does not contain "@cloudflare" or "cloudflare()"
   - vite.config.ts plugins array is `[react(), tailwindcss()]`
   - index.html title is "Murder Mystery Memo"
     </acceptance_criteria>
     <done>All dead packages removed, @xyflow/react installed, scripts cleaned, vite config simplified, HTML title updated</done>
     </task>

<task type="auto">
  <name>Task 2: Remove dead source code and rewire entry points</name>
  <files>
    src/app/index.tsx,
    src/app/routes/routes.tsx,
    src/app/styles/index.css,
    src/shared/api/index.ts,
    src/pages/canvas/ui/CanvasPage.tsx,
    src/pages/canvas/index.ts
  </files>
  <read_first>
    - src/app/index.tsx (current Clerk/Convex providers to remove)
    - src/app/routes/routes.tsx (current Clerk auth logic to remove)
    - src/app/styles/index.css (fontsource import to remove)
    - src/shared/api/index.ts (Convex re-exports to remove)
    - src/pages/home/ui/HomePage.tsx (current page to understand what exists)
    - src/pages/home/index.ts (current export)
    - src/app/providers/ErrorBoundary/ErrorBoundary.tsx (verify it has no Clerk/Convex deps)
  </read_first>
  <action>
1. Delete the following directories and files entirely (per D-07, D-08, D-09, D-10):
   ```bash
   rm -rf src/features/todo/
   rm -rf src/shared/api/convex/
   rm -rf src/widgets/header/
   rm -f e2e/todo.spec.ts
   rm -f convex.json
   rm -f wrangler.jsonc
   rm -f .env.dev .env.prod .env.example
   ```

2. Delete `src/shared/api/index.ts` (its only exports were Convex-related).

3. Rename pages/home to pages/canvas (per D-13):

   ```bash
   mv src/pages/home src/pages/canvas
   ```

4. Rewrite `src/pages/canvas/ui/CanvasPage.tsx` (was HomePage.tsx, rename file):

   ```bash
   mv src/pages/canvas/ui/HomePage.tsx src/pages/canvas/ui/CanvasPage.tsx
   ```

   Content:

   ```tsx
   export const CanvasPage = () => {
     return <div className="h-full w-full bg-neutral-950">Canvas placeholder</div>;
   };
   ```

   This is a temporary placeholder. Plan 01-02 will add ReactFlow here.

5. Rewrite `src/pages/canvas/index.ts`:

   ```typescript
   export { CanvasPage } from "./ui/CanvasPage";
   ```

6. Rewrite `src/app/index.tsx` to remove all Clerk/Convex imports and providers. Final content:

   ```tsx
   import "./styles/index.css";
   import { StrictMode } from "react";
   import { createRoot } from "react-dom/client";
   import { ErrorBoundary } from "@/app/providers/ErrorBoundary";
   import { Routes } from "./routes";

   const rootElement = document.getElementById("root");
   if (rootElement === null) throw new Error("Root element not found");

   createRoot(rootElement).render(
     <StrictMode>
       <ErrorBoundary>
         <Routes />
       </ErrorBoundary>
     </StrictMode>,
   );
   ```

   Note: Replaces `document.getElementById("root")!` (non-null assertion) with explicit null check (per research Pitfall 3).

7. Rewrite `src/app/routes/routes.tsx` to remove Clerk auth logic (per D-11). Final content:

   ```tsx
   import { CanvasPage } from "@/pages/canvas";

   export const Routes = () => {
     return <CanvasPage />;
   };
   ```

8. Edit `src/app/styles/index.css`:
   - Remove the line `@import "@fontsource-variable/noto-sans";` (per D-04)
   - Change `--font-sans: "Noto Sans Variable", sans-serif;` to `--font-sans: sans-serif;` (temporary, Phase 4 will add proper fonts)
     </action>
     <verify>
     <automated>cd /Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/murder-mystery-memo && bun run typecheck && echo "typecheck passed" && ! test -d src/features/todo && echo "todo deleted" && ! test -d src/shared/api/convex && echo "convex deleted" && ! test -d src/widgets/header && echo "header deleted" && test -f src/pages/canvas/ui/CanvasPage.tsx && echo "canvas page exists" && grep "CanvasPage" src/app/routes/routes.tsx && echo "routes wired"</automated>
     </verify>
     <acceptance_criteria>
   - `bun run typecheck` exits 0 with no errors
   - src/features/todo/ directory does not exist
   - src/shared/api/convex/ directory does not exist
   - src/shared/api/index.ts does not exist
   - src/widgets/header/ directory does not exist
   - e2e/todo.spec.ts does not exist
   - convex.json does not exist
   - wrangler.jsonc does not exist
   - .env.dev, .env.prod, .env.example do not exist
   - src/pages/home/ directory does not exist
   - src/pages/canvas/ui/CanvasPage.tsx exists and exports `CanvasPage`
   - src/pages/canvas/index.ts exports `CanvasPage` from `./ui/CanvasPage`
   - src/app/index.tsx does NOT contain "clerk", "convex", "ClerkProvider", "ConvexProvider", "as string", or "!"
   - src/app/index.tsx contains `document.getElementById("root")` with null check (no non-null assertion)
   - src/app/routes/routes.tsx does NOT contain "clerk", "useAuth", "SignIn"
   - src/app/routes/routes.tsx imports from `@/pages/canvas`
   - src/app/styles/index.css does NOT contain "fontsource"
     </acceptance_criteria>
     <done>All dead code removed, entry points rewired to CanvasPage, typecheck passes cleanly</done>
     </task>

</tasks>

<verification>
1. `bun run typecheck` passes with zero errors
2. `bun run build` succeeds
3. No references to convex, clerk, cloudflare, wouter, next-themes, dotenvx in any source file under src/
4. `grep -r "convex\|@clerk\|cloudflare\|wouter\|next-themes\|dotenvx" src/` returns no matches
</verification>

<success_criteria>

- The project builds and type-checks cleanly
- All dead dependencies are removed from package.json and lock file
- All source files referencing removed packages are deleted or rewritten
- @xyflow/react is installed and available
- pages/home is renamed to pages/canvas with CanvasPage component
- Entry point renders CanvasPage through Routes without any auth/backend providers
  </success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-foundation/01-01-SUMMARY.md`
</output>
